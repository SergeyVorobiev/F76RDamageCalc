// As reducer helps with scalability, it does not help with not re-rendering everything just because you have indirect not related
// dependencies from items which you can't avoid to use by design.
// import RApp from './experiments/reducerExample/RApp';
// <RApp></RApp>


export default function TApp() {
    return (
        <>
            <div>Tests</div>
        </>
    );
}